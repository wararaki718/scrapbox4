name: evaluator
description: evaluation

outputs:
  - { name: metrics_path, type: String, description: "show metrics" }

implementation:
  container:
    image: sample-metrics:latest
    command: [
      python,
      main.py,
      --metricspath,
      { outputPath: metrics_path }
    ]
