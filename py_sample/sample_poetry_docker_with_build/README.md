# sample docker image based on poetry

## build

```shell
docker-compose build
```

## run

```shell
docker-compose up
```

## test

```shell
docker-compose -f docker-compose.yml -f docker-compose.test.yml up
```
